import React from 'react';

const toolbar = props => (
    <header className='Toolbar'>
        <div>MENU</div>
        <div>LOGO</div>
        <nav>

        </nav>
    </header>
);

export default toolbar;